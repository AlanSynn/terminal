FROM archlinux:latest
RUN pacman -Sy --noconfirm bash ca-certificates curl git sudo && pacman -Scc --noconfirm
WORKDIR /workspace
COPY . .
RUN BOOTSTRAP_DRY_RUN=1 bash -n bootstrap.sh scripts/*.sh scripts/lib/*.sh test/*.sh
RUN ./bootstrap.sh --dry-run --tier ci
