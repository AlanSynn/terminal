#!/usr/bin/env bash
set -euo pipefail
if [[ "${CHEZMOI_VERIFY:-0}" == "1" ]]; then
  "{{ .chezmoi.sourceDir }}/scripts/verify-new-machine.sh" --tier "${BOOTSTRAP_TIER:-minimal}"
else
  echo "Skipping postinstall verification; run just verify or set CHEZMOI_VERIFY=1."
fi
